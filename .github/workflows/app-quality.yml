name: App Quality Checks

on:
  push:
    paths:
      - 'app/**'
      - 'supabase/**'
      - '.github/workflows/app-quality.yml'
  pull_request:
    paths:
      - 'app/**'
      - 'supabase/**'
      - '.github/workflows/app-quality.yml'

jobs:
  quality:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: app

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: app/package-lock.json

      - name: Setup Deno
        uses: denoland/setup-deno@v2
        with:
          deno-version: v2.x

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Unit tests
        run: npm run test

      - name: Edge Function unit tests
        working-directory: .
        run: deno test --allow-env --allow-read=supabase/functions supabase/functions/tests/*.ts

      - name: Build
        run: npm run build
